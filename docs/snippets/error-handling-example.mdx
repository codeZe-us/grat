```typescript
try {
  const result = await grat.sponsor(transaction);
  console.log("Sponsored hash:", result.hash);
} catch (error) {
  if (error instanceof GratError) {
    console.error(`Relay error [${error.code}]: ${error.message}`);
  } else {
    console.error("Unexpected error:", error);
  }
}
```
