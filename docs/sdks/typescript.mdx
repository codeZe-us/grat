---
title: TypeScript SDK
description: "The official JavaScript/TypeScript client for the Grat Relay."
---

import installSdk from '/snippets/install-sdk.mdx'
import errorExample from '/snippets/error-handling-example.mdx'

## Installation

<installSdk />

## Initialization

You can initialize the client using the constructor or the static shortcuts.

<CodeGroup>
```typescript Testnet
import { Grat } from '@grat-official-sdk/sdk';

const grat = Grat.testnet(); 
// or custom URL
// const grat = Grat.testnet('https://my-relay.com');
```

```typescript Mainnet
import { Grat } from '@grat-official-sdk/sdk';

const grat = Grat.mainnet('sr_live_...', 'https://api.grat.network');
```
</CodeGroup>

## Methods

### `sponsor(transaction)`

Wraps a signed transaction in a fee-bump envelope and submits it.

| Param | Type | Description |
| :--- | :--- | :--- |
| `transaction` | `Transaction \| FeeBumpTransaction` | A signed Stellar transaction object. |

**Returns**: `Promise<SponsorResult>`

### `simulate(transaction)`

Simulates a Soroban transaction to get resource estimates.

| Param | Type | Description |
| :--- | :--- | :--- |
| `transaction` | `Transaction` | An unsigned or signed Stellar transaction. |

**Returns**: `Promise<SimulationResult>`

### `estimate(transaction)`

Predicts the total fee cost for a transaction without submitting it.

**Returns**: `Promise<EstimateResult>`

### `status()`

Checks the health of the relay server.

**Returns**: `Promise<HealthStatus>`

## Error Handling

The SDK throws typed errors that you can catch to handle specific scenarios:

- **FrozenEntryError** (422) - Thrown when the sponsored transaction references a ledger entry that has been frozen by network consensus (Protocol 26, CAP-0077). This is a network-level security measure, not a problem with the transaction itself.
- **InsufficientCreditsError** (402) - The project has run out of gas credits.
- **RateLimitError** (429) - You have exceeded the relay's rate limit.
- **SubmissionFailedError** (400) - The transaction was rejected by the Stellar network (e.g., bad sequence, op_failed).
- **ValidationError** (400) - The XDR or request parameters are invalid.

<errorExample />
